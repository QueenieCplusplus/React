// var React = require('react');
var ReactDOM = require('react-dom');

// (p)=>{} is a function in JS
// below is a stateless func
const sum = (a, b, c) => a + b + c;

let a = 0;
const sum = () => a++;

ReactDOM.render();